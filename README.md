## Requirements:
Linux.

sudo apt-get -y update \
    && apt-get install -y \
        fonts-font-awesome \
        libcairo2 \
        libffi-dev \
        libgdk-pixbuf2.0-0 \
        libpango1.0-0 \
        python-dev \
        python-lxml \
        shared-mime-info \
        net-tools\
    && apt-get -y clean

